{
  "name": "service-oracle",
  "version": "1.0.0",
  "description": "A service locator for NodeJS and the browser.",
  "keywords": [
    "service",
    "locator",
    "registry",
    "container",
    "di",
    "inject",
    "pattern"
  ],
  "repository": {
    "type": "git",
    "url": "git@github.com:digitaledgeit/service-oracle.git"
  },
  "bugs": {
    "url": "https://github.com/digitaledgeit/service-oracle/issues"
  },
  "devDependencies": {
    "coveralls": "^2.11.4",
    "istanbul": "^0.3.22",
    "mocha": "^2.2.5",
    "mocha-istanbul": "^0.2.0"
  },
  "scripts": {
    "test": "mocha",
    "coverage": "istanbul cover _mocha --report html --dir test/__coverage__",
    "coveralls": "istanbul cover _mocha --report lcovonly --dir test/__coverage__ && coveralls < test/__coverage__/lcov.info"
  },
  "license": "MIT"
}
